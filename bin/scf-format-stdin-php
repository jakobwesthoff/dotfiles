#!/bin/bash

tmpdir="$(mktemp -d)"
FORMAT_TMP_PATH="$tmpdir/input.php"
trap 'rm -rf "$tmpdir"' EXIT

cat > "$FORMAT_TMP_PATH"

cd "$(dirname "${FORMAT_TMP_PATH}")" || exit 1
/Users/jakob/Development/gitlab/ekkogmbh/scf/scripts/docker-wrapper.sh format "$FORMAT_TMP_PATH"
